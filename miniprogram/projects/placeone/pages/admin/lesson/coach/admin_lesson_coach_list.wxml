<block wx:if="{{isAdmin&&isLoad}}">
	<cmpt-comm-list source='admin' type="admin-lesson-coach-list" search="{{search||''}}" _menus="{{sortMenus}}" _items="{{sortItems}}" route="admin/lesson_coach_list" sortMenusDefaultIndex="0" topBottom="50" placeholder="搜索教练姓名" bind:list="bindCommListCmpt">
		<view slot="searchEnd">
			<button class="btn-mini" bindtap="seedDemo">示例数据</button>
		</view>
		<template is="listLoadTpl" data="{{skin:'load-project',dataList}}" />
	</cmpt-comm-list>
</block>