<block wx:if="{{isAdmin&&isLoad}}">
	<cmpt-comm-list source='admin' type="admin-lesson-order-list" search="{{search||''}}" _menus="{{sortMenus}}" _items="{{sortItems}}" route="admin/lesson_order_list" sortMenusDefaultIndex="0" topBottom="50" placeholder="按用户/日期搜索" bind:list="bindCommListCmpt">
		<view slot="searchEnd">
			<button class="btn-mini" bindtap="exportData">导出</button>
		</view>
		<template is="listLoadTpl" data="{{skin:'load-project',dataList}}" />
	</cmpt-comm-list>
</block>